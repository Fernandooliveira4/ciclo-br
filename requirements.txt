# As versoes que o app publicado roda. O Streamlit Cloud instala deste arquivo,
# por deteccao automatica, e por isso ele e a referencia: os workflows instalam
# dele tambem (via requirements-ci.txt) para que a CI teste o que o site roda.
#
# Ate 2026-09-13 este comentario afirmava que o GitHub Actions usava este
# arquivo, e nao usava: os workflows resolviam as faixas do pyproject. Estavam
# rodando versoes diferentes das do site.
#
# Versoes verificadas em 2026-09-12 com Python 3.12.10.
requests==2.34.2
tenacity==9.1.4
PyYAML==6.0.3
pandas==3.0.5
pyarrow==25.0.1
duckdb==1.5.5
statsmodels==0.15.0
# O painel. Altair vem junto com o Streamlit, mas fica fixado aqui porque os
# gráficos dependem dele diretamente.
streamlit==1.63.0
altair==6.2.2
